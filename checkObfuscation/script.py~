import sys
import re

#prendo l'input temporaneamente un file specifico cioè MainAcctivity.smali

path=sys.argv[1]

in_file=open(path,"r")
text =in_file.read()
in_file.close()

pattern="\s*.method\spublic\s([\w\.]+)\(\)[\(.]*"
with open(path) as infile:
    for line in infile:
        m=re.fullmatch(pattern,ù
                       line)
        if m:
            print m
